export declare function is_soffice_installed(): boolean;
export type Format = 'pdf' | 'html' | 'doc' | 'docx' | 'odt' | 'odp' | 'pptx' | 'ppt' | 'ods' | 'xlsx' | 'xls';
/**
 * Convert a file to a different format using LibreOffice cli `soffice`.
 *
 * @param options.input_file - The path to the input file.
 * @param options.convert_to - The format to convert the file to.
 *
 * @returns The path to the output file.
 */
export declare function convertTo(options: {
    input_file: string;
    convert_to: Format;
}): Promise<string>;
export declare function convertToPDF(input_file: string): Promise<string>;
export declare function convertToHTML(input_file: string): Promise<string>;
export declare class ChildProcessError extends Error {
    code: number | null;
    stdout: string;
    stderr: string;
    constructor(message: string, code: number | null, stdout: string, stderr: string);
}
